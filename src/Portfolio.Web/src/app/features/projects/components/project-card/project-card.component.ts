import { ChangeDetectionStrategy, Component, input } from '@angular/core';
import { RouterLink } from '@angular/router';
import { ProjectListItem } from '../../data-access/project.models';
@Component({selector:'app-project-card',imports:[RouterLink],changeDetection:ChangeDetectionStrategy.OnPush,template:`<article class="group flex h-full flex-col overflow-hidden rounded-[20px] border border-border bg-white shadow-sm transition duration-200 hover:-translate-y-1 hover:shadow-xl"><a [routerLink]="['/projects',project().slug]" class="relative block aspect-[16/10] overflow-hidden bg-gradient-to-br from-violet-600 to-indigo-950">@if(project().thumbnailUrl){<img [src]="project().thumbnailUrl" [alt]="project().title" loading="lazy" class="size-full object-cover transition duration-300 group-hover:scale-105">}@else{<div class="grid size-full place-items-center text-white/70"><span class="text-5xl font-black">{{project().title.charAt(0)}}</span></div>}@if(project().isFeatured){<span class="absolute start-4 top-4 rounded-full bg-amber-300 px-3 py-1 text-xs font-bold text-amber-950">Featured</span>}</a><div class="flex grow flex-col p-5"><div class="flex flex-wrap gap-2">@for(tech of project().technologies.slice(0,4);track tech.id){<span class="rounded-full bg-primary/10 px-2.5 py-1 text-xs font-semibold text-primary">{{tech.name}}</span>}</div><h2 class="mt-4 text-xl font-bold tracking-tight"><a [routerLink]="['/projects',project().slug]" class="hover:text-primary">{{project().title}}</a></h2><p class="mt-3 line-clamp-3 grow text-sm leading-6 text-muted">{{project().shortDescription}}</p><a [routerLink]="['/projects',project().slug]" class="mt-5 inline-flex items-center gap-2 text-sm font-bold text-primary">View case study <span aria-hidden="true">→</span></a></div></article>`})
export default class ProjectCardComponent{readonly project=input.required<ProjectListItem>()}
