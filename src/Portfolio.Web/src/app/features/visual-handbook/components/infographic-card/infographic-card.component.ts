import { ChangeDetectionStrategy, Component, input } from '@angular/core';
import { RouterLink } from '@angular/router';
import { InfographicListItem, difficultyLabel } from '../../data-access/infographic.models';
@Component({selector:'app-infographic-card',imports:[RouterLink],changeDetection:ChangeDetectionStrategy.OnPush,template:`<article class="group flex h-full flex-col overflow-hidden rounded-[20px] border border-slate-200 bg-white shadow-sm transition duration-200 hover:-translate-y-1 hover:border-violet-300 hover:shadow-xl"><a [routerLink]="['/visual-handbook',item().slug]" class="relative block aspect-[16/9] overflow-hidden bg-gradient-to-br from-slate-950 via-indigo-950 to-violet-800">@if(item().coverUrl){<img [src]="item().coverUrl" [alt]="item().title" loading="lazy" class="size-full object-cover transition duration-300 group-hover:scale-[1.02]">}@else{<div class="absolute inset-0 opacity-30" aria-hidden="true" style="background-image:linear-gradient(rgba(255,255,255,.12) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.12) 1px,transparent 1px);background-size:32px 32px"></div><div class="relative flex size-full items-end p-5"><span class="text-3xl font-black text-white/90">{{item().category.name}}</span></div>}</a><div class="flex grow flex-col p-5"><div class="flex flex-wrap items-center gap-2 text-xs font-bold uppercase tracking-wide"><span class="text-violet-700">{{item().category.name}}</span><span class="text-slate-300">•</span><span class="text-slate-500">{{difficulty(item().difficultyLevel)}}</span>@if(item().isFeatured){<span class="ms-auto rounded-full bg-amber-100 px-2 py-1 text-amber-800">Featured</span>}</div><h3 class="mt-4 text-xl font-black leading-tight text-slate-950"><a [routerLink]="['/visual-handbook',item().slug]" class="focus-visible:outline focus-visible:outline-2 focus-visible:outline-violet-600">{{item().title}}</a></h3><p class="mt-3 grow text-sm leading-6 text-slate-600">{{item().shortDescription}}</p><div class="mt-5 flex flex-wrap gap-1.5">@for(tag of item().tags.slice(0,4);track tag.id){<span class="rounded-full bg-violet-50 px-2.5 py-1 text-xs font-semibold text-violet-800">{{tag.name}}</span>}</div><a [routerLink]="['/visual-handbook',item().slug]" class="mt-6 inline-flex min-h-11 items-center font-bold text-violet-700">Open visual guide <span class="ms-2" aria-hidden="true">→</span></a></div></article>`})
export default class InfographicCardComponent{readonly item=input.required<InfographicListItem>();readonly difficulty=difficultyLabel}
