import { ChangeDetectionStrategy, Component, EventEmitter, Input, Output } from '@angular/core';
export interface PickableMedia { id:string; originalFileName:string; url:string; contentType?:string; mimeType?:string }
@Component({selector:'app-media-picker',changeDetection:ChangeDetectionStrategy.OnPush,template:`@if(open){<div class="fixed inset-0 z-50 grid place-items-center bg-black/60 p-4" role="dialog" aria-modal="true" aria-labelledby="picker-title" (click)="dismissed.emit()"><section class="max-h-[85vh] w-full max-w-4xl overflow-auto rounded-2xl bg-background p-6 shadow-2xl" (click)="$event.stopPropagation()"><div class="flex items-center justify-between"><div><h2 id="picker-title" class="text-xl font-semibold">Select media</h2><p class="text-sm text-muted-foreground">Choose a reusable portfolio file.</p></div><button class="kt-btn kt-btn-icon kt-btn-ghost" aria-label="Close media picker" (click)="dismissed.emit()">×</button></div><label class="mt-5 block"><span class="sr-only">Search media</span><input class="kt-input" placeholder="Search files" (input)="filter=$any($event.target).value"></label><div class="mt-5 grid grid-cols-2 gap-3 md:grid-cols-4">@for(item of visible();track item.id){<button type="button" class="overflow-hidden rounded-xl border border-border text-start hover:border-primary focus-visible:outline focus-visible:outline-2 focus-visible:outline-primary" (click)="selected.emit(item)"><div class="grid aspect-video place-items-center bg-muted">@if(mediaType(item).startsWith('image/')){<img [src]="item.url" [alt]="item.originalFileName" class="size-full object-cover">}@else{<i class="ki-filled ki-document text-4xl text-primary"></i>}</div><strong class="block truncate p-3 text-sm">{{item.originalFileName}}</strong></button>}</div></section></div>}`})
export class MediaPickerComponent{@Input() open=false;@Input() items:PickableMedia[]=[];@Input() accept:'image'|'pdf'|'all'='all';@Output() selected=new EventEmitter<PickableMedia>();@Output() dismissed=new EventEmitter<void>();filter='';mediaType(item:PickableMedia){return item.contentType??item.mimeType??''}visible(){return this.items.filter(x=>(this.accept==='all'||(this.accept==='image'?this.mediaType(x).startsWith('image/'):this.mediaType(x)==='application/pdf'))&&x.originalFileName.toLowerCase().includes(this.filter.toLowerCase()))}}
